\chapter{Algunos comandos b\'asicos para POY}
\label{ch:usoPOY}
\section{An\'alisis de sensibilidad con costos diferenciales}
\small
\noindent
% 
(* tomado de url{groups.google.com/group/POY4/}      *)\\
(* dados dos archivos de datos                    *)\\
(* 1.fas y 3.fas                                  *)
\Cmdi{\begin{itemize}[label=$>$]
         \item read($"$1.fas$"$, $"$3.fas$"$)
         \item transform((names:($"$1.fas$"$), tcm:$"$412.txt$"$), 
         (names:($"$3.fas$"$), tcm:$"$121.txt$"$))
         \item store($"$misdatos$"$)
         \item build(5, trees:2)
         \item select(best:1)
         \item transform((all, static\_approx))
         \item report($"$todo13.mtr$"$, phastwinclad)
         (* Resultados en el archivo Sensitivity\_results.txt *)
         \item echo ($"$412+121 Resultados$"$, output:$"$Sensitivity\_results.txt$"$)
         \item report ($"$Sensitivity\_results.txt$"$, treestats)\\
         (* Deseche los \'arboles *)
         \item select(best:0)
         \item use($"$misdatos$"$)
         \item select(characters,names:($"$1.fas$"$))
         \item build(5)
         \item select()
         \item echo($"$1.fas Resultados$"$, 
         output:$"$Sensitivity\_results.txt$"$)
         \item report($"$Sensitivity\_results.txt$"$, treestats)
         (* Deseche los \'arboles *)
         \item transform((all, static\_approx))
         \item report($"$matriz1.mtr$"$, phastwinclad)
         \item use($"$misdatos$"$)
         \item select(best:0)
         \item select(characters,names:($"$3.fas$"$))
         \item build(5)
         \item select(best:1)
         \item echo ($"$3.fas Resultados$"$, 
         output:$"$Sensitivity\_results.txt$"$)
         \item report ($"$Sensitivity\_results.txt$"$, treestats)
         \item transform((all, static\_approx))
         \item report ($"$matriz3.mtr$"$, phastwinclad)
         \item quit()
       \end{itemize} 
       }